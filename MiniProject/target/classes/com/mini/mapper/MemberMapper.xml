<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mini.mapper.MemberMapper">
	<resultMap id="authMap" type="com.mini.domain.AuthVO">
		<result property="username" column="username" />
		<result property="auth" column="auth" />
	</resultMap>
	<resultMap id="memberMap"
		type="com.mini.domain.MemberVO">
		<id property="username" column="username" />
		<result property="username" column="username" />
		<result property="password" column="password" />
		<result property="name" column="name" />
		<result property="nickname" column="nickname"/>
		<result property="cellPhone" column="cell_phone"/>
		<result property="email" column="email" />
		<result property="regDate" column="reg_date" />
		<result property="updateDate" column="update_date" />
	</resultMap>
	<select id="read" resultMap="memberMap">
		select
			m.username, password, nickname, cell_phone, email, reg_date, update_date
		from tbl_member m left outer join tbl_member_auth a
			on m.username = a.username
		where m.username = #{username}
	</select>
	<insert id="insert">
		insert into tbl_member (username, password, name, nickname, cell_phone, email)
		values (#{username}, #{password}, #{name}, #{nickname}, #{cellPhone}, #{email})
	</insert>
	<insert id="insertAuth">
		insert into tbl_member_auth (username, auth)
		values (#{username}, #{auth})
	</insert>
</mapper>